---
// Data of the cards of the articles
const {name, category, description, image} = Astro.props;
---

<div class="product-card flex flex-col justify-between bg-neutral-0 rounded-2xl font-regular shadow-lg p-2">
    <a href={} class="flex flex-col gap-2 text-neutral-100">
        <img src={image} alt={name} class="w-full h-2/3 object-cover rounded-lg">
        <div class="flex flex-col gap-1 p-2">
            <p class="bg-state-blue w-1/3 rounded-full font-bold text-neutral-0 px-4 py-1 text-[10px] lg:text-xs">{category}</p>
            <h3 class="text-base font-bold ">{name}</h3>
            <p class="flex-grow text-neutral-60 line-clamp-3 text-xs lg:text-base">
                {description}
            </p>   
        </div>
    </a>
</div>